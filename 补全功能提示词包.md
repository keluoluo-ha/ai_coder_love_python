# AI 补全功能 · 简洁提示词包

> 把每条直接交给 AI 执行即可。技术栈固定：FastAPI + LangChain(ChatTongyi) + PGVector + SQLAlchemy/pymysql + Pydantic；模型 qwen-plus / text-embedding-v2。只新增、不改已有逻辑；密钥走 `app/config.py` 的 `.env`；工具继承 `BaseTool`，注册进 `support_agent.py` 的 `ToolCollection`。

---

**任务0（地基）｜修 `app/db/models.py` 编码**
该文件是 UTF-16，Python 无法 import。转成 UTF-8（无 BOM）重存，内容不变。验收：`python -c "import app.db.models"` 通过。

**任务1｜补 5 个工具**
新建并按 `WebSearchTool.py` 写法实现，注册进 `SupportAgent`：
- `FileOperationTool`(file_operation)：read/write/list，带路径穿越防护
- `PDFGenerationTool`(pdf_generate)：reportlab 生成恋爱报告 PDF，返回路径
- `ResourceDownloadTool`(resource_download)：下载 url
- `TerminalOperationTool`(terminal)：subprocess 执行，加命令黑名单
- `WebScrapingTool`(web_scraping)：requests+BeautifulSoup 抓正文
验收：`tool_collection.execute("pdf_generate",{...})` 能生成文件。

**任务2｜恋爱咨询业务域**
`db/models.py` 新增 `ConsultationRecord/ConsultationFollowUp/EmotionProfile` 三表；新建 Pydantic `schemas_consultation.py`(Start/Complete/CompleteFollowUp/Skip 请求 + 3 个 VO)；新建 `services/`(Consultation/FollowUp/EmotionProfile 三个 Service，用 SQLAlchemy session)；新建 `domain/consultation_effect_evaluator.py`(0~100 效果分)；新建 `controller/consultation_controller.py` 暴露 8 个端点（对齐 Java `EmotionalCareController`），挂到 `main.py`。验收：8 个端点能跑通并落 MySQL。

**任务3｜情感关怀异步（RabbitMQ）**
新建 `mq/`(Event/Publisher/Consumer，用 pika，配置读 `RABBITMQ_URL`) + `services/emotional_care_post_process.py`；在任务2 的 `complete()` 里 publish 事件，消费者生成关怀文案并写 follow_up/emotion_profile。验收：咨询完成后 MQ 收到事件并落库。

**任务4｜RAG 检索增强**
新建 `token_text_splitter.py`(MyTokenTextSplitter，按 token 切)、`keyword_enricher.py`(MyKeywordEnricher)、`query_expander.py`(QueryExpander)、`rewrite_transformer.py`(RewriteTransformer)；把 `vector_store.load_all_markdown` 的切分器换成 token 版并加关键词增强；`RAGSearchTool.execute` 改为 扩展→改写→多 query 检索合并。验收：检索更稳。

**任务5｜Agent 运行态持久化（续跑）**
新建 `agent/run_state_store.py`(AgentRunState + AgentRunStateStore 抽象 + InMemory 实现)；`SupportAgent` 支持 `run_id` 恢复 memory/steps/state；`manus_controller` 新增 `POST /api/ai/manus/reply` 接收回答后带着原上下文续跑。验收：ask_human 后凭 runId 续跑不重来。

**任务6｜Advisor**
新建 `advisor/logger_advisor.py`(LLM 调用日志，挂 `llm.py`) + `advisor/rereading_advisor.py`(重读提示，拼进 `prompts.py`)。验收：控制台有日志、回答更贴约束。

**任务7｜文件服务接口**
新建 `controller/file_controller.py`：`GET /api/files/pdf/list` + `GET /api/files/pdf/{filename}`(安全下载，防 `..`)。config 加 `PDF_SAVE_DIR`。验收：能列能下。

**任务8｜RAG 离线评测**
新建 `rag/eval/`：`RagEvalCase`/`DatasetLoader`/`RetrievalEvaluator`(Hit@k, MRR, Recall)/`ReportWriter`/`__main__`。验收：`python -m app.rag.eval` 出报告。

---
顺序：0→1→2→3→4→5→6→7→8
